{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Frontend Dev Server",
      "type": "shell",
      "command": "echo 'Waiting for npm install to complete...' && while [ ! -f /workspace/frontend/.npm-ready ]; do sleep 2; done && cd /workspace/frontend && PORT=3000 npm run dev -- --host",
      "isBackground": true,
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      },
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
